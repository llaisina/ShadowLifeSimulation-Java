import bagel.util.Point;
/**
 * Represent a Pad entity which extends from Sprites
 */
public class Pad extends Sprites {
    public Pad(Point point) {
        super("res/images/pad.png", point);
    }
}
