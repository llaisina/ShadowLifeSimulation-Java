import bagel.util.Point;
/**
 * Represent a Golden Tree entity which extends from Sprites
 */
public class GoldTree extends Sprites {
    public GoldTree(Point point) {
        super("res/images/gold-tree.png", point);
    }
}
