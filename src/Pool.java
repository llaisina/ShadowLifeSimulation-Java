import bagel.util.Point;
/**
 * Represent a Pool entity which extends from Sprites
 */
public class Pool extends Sprites {
    public Pool(Point point) {
        super("res/images/pool.png", point);
    }
}
